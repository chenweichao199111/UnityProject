using UnityEngine;
using Framework.Unity.Sqlite;

public class Test : MonoBehaviour
{

    // Use this for initialization
    void Start()
    {
        var t = SQLiteManager.Instance;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
